# E_T_L_SYSTEM
Scalable job import system using Node.js, MongoDB, Redis (BullMQ), and Next.js. Fetches XML job feeds, converts to JSON, queues jobs for background processing, performs upserts into MongoDB, and tracks import history (new/updated/failed). Includes admin UI for monitoring imports and logs.
